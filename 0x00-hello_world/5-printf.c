#include <stdio.h>
/**
 * writing C program that prints a lettre
 * using printf not puts
 * and returning 0 
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
